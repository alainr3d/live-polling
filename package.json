{
  "name": "workspace",
  "version": "1.0.0",
  "description": "live polling app",
  "main": "index.js",
  "scripts": {
    "prestart": "babel ./es6 --out-dir .;webpack",
    "start": "node app-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^0.14.0",
    "react-dom": "^0.14.0"
  }
}
